name: docker-kitsu

networks:
  web:
    external: false
  internal:
    external: false

volumes:
  kitsu_pgdata:
  kitsu_traefik_letsencrypt:
  kitsu_media:
  kitsu_backups:

services:
  traefik:
    image: traefik:v3.1
    command:
      - --api.dashboard=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:${TRAEFIK_HTTP_PORT}
      - --entrypoints.websecure.address=:${TRAEFIK_HTTPS_PORT}
      - --certificatesresolvers.le.acme.email=${LETSENCRYPT_EMAIL}
      - --certificatesresolvers.le.acme.storage=/letsencrypt/acme.json
      - --certificatesresolvers.le.acme.httpchallenge=true
      - --certificatesresolvers.le.acme.httpchallenge.entrypoint=web
      # Redirect HTTP -> HTTPS
      - --entrypoints.web.http.redirections.entryPoint.to=websecure
      - --entrypoints.web.http.redirections.entryPoint.scheme=https
    ports:
      - "${TRAEFIK_HTTP_PORT}:80"
      - "${TRAEFIK_HTTPS_PORT}:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - kitsu_traefik_letsencrypt:/letsencrypt
      - ./traefik/traefik.yml:/traefik.yml:ro
    networks:
      - web
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      # (Optional) protect the dashboard by host rule or remove in production
      # Access: https://kitsu.example.com:443/ (only if you add a router for it)
      # Better approach: don't expose dashboard publicly.

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - kitsu_pgdata:/var/lib/postgresql/data
    networks:
      - internal
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10

  # ---- ZOU (backend API) ----
  zou-api:
    build:
      context: ./zou
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
      REDIS_URL: ${REDIS_URL}
      ZOU_SECRET_KEY: ${ZOU_SECRET_KEY}
      ZOU_ALLOWED_HOSTS: ${ZOU_ALLOWED_HOSTS}
      ZOU_LOG_LEVEL: ${ZOU_LOG_LEVEL}
      LOG_LEVEL: ${ZOU_LOG_LEVEL}
      ZOU_WORKERS: ${ZOU_WORKERS}
      ZOU_THREADS: ${ZOU_THREADS}
      SECRET_KEY: ${ZOU_SECRET_KEY}
      DB_HOST: db
      DB_PORT: 5432
      DB_USERNAME: kitsu
      DB_PASSWORD: change_me_strong
      DB_DATABASE: kitsu

      KV_HOST: redis
      KV_PORT: 6379
    depends_on:
      - db
    networks:
      - internal
      - web
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.zou-api.rule=Host(`${KITSU_DOMAIN}`) && PathPrefix(`/api`)"
      - "traefik.http.routers.zou-api.entrypoints=websecure"
      - "traefik.http.routers.zou-api.tls.certresolver=le"
      - "traefik.http.services.zou-api.loadbalancer.server.port=8000"

  # ---- KITSU (frontend) ----
  kitsu-web:
    build:
      context: ./kitsu
    environment:
      # The frontend needs to know the public API URL
      PUBLIC_API_URL: ${KITSU_API_URL}
    depends_on:
      - zou-api
    networks:
      - internal
      - web
    restart: unless-stopped
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.kitsu-web.rule=Host(`${KITSU_DOMAIN}`)"
      - "traefik.http.routers.kitsu-web.entrypoints=websecure"
      - "traefik.http.routers.kitsu-web.tls.certresolver=le"
      - "traefik.http.middlewares.strip-api.stripprefix.prefixes=/api"
      - "traefik.http.services.kitsu-web.loadbalancer.server.port=80"

  # ---- Daily DB backups ----
  db-backup:
    image: postgres:15
    depends_on:
      - db
    environment:
      PGPASSWORD: ${POSTGRES_PASSWORD}
      PGUSER: ${POSTGRES_USER}
      PGDATABASE: ${POSTGRES_DB}
      PGHOST: db
      BACKUP_CRON: ${BACKUP_CRON}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS}
    volumes:
      - kitsu_backups:/backups
      - ./backups/backup.sh:/backup.sh:ro
    entrypoint: ["/bin/bash", "-c", "chmod +x /backup.sh && /backup.sh"]
    networks:
      - internal
    restart: unless-stopped
